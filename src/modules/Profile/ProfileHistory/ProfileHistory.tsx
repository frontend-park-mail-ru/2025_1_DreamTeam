// filepath: /home/damir/vdom-tutorial/src/modules/Profile/ProfileHistory/ProfileHistory.tsx

import styles from "./ProfileHistory.module.scss";

const ProfileHistory = () => {
  return (
    <div class={styles.content}>
      Контент для ProfileHistory(Здесь пока ничего нет)
    </div>
  );
};

export default ProfileHistory;
